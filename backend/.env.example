# PostgreSQL connection string — Railway provides this automatically when you add a Postgres plugin
DATABASE_URL=postgresql://user:password@host:5432/dbname

# OpenAI key for the agent chat feature
OPENAI_API_KEY=sk-...

# Secret key used to sign JWT tokens (generate with: openssl rand -hex 32)
SECRET_KEY=change-me-to-a-random-secret
