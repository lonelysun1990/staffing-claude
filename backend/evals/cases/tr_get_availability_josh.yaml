id: tr_get_availability_josh
fixture: fixtures/full_store.json
runs: 1
user_message: What is Josh Zhang's availability (free percentage) week by week for the full horizon? Answer in one short paragraph.
expect:
  must_call_tools:
    - "mcp__staffing__get_availability"
  max_tool_calls: 25
